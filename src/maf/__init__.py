"""Implementation 1: the Microsoft Agent Framework.

The agent is built in memory and lives only as long as the process:

    python -m src.maf.main
    python -m src.maf.examples.read_only

Start with `agent.py`. The Foundry Agent Service version of the same agent is
in `src/foundry/`; docs/implementations.md compares the two.
"""
